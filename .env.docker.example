# 复制为 .env.docker 并填入真实值（.env.docker 已被 gitignore，不会进仓库）
# ── 必填 ──────────────────────────────────────────────
# 管理员登录密码（生产未配置时登录接口直接拒绝）
ADMIN_PASSWORD=请改成强密码

# ── 建议 ──────────────────────────────────────────────
# 站点公开地址（sitemap / RSS / OG 分享卡片的 URL 拼接）
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# ── 可选 ──────────────────────────────────────────────
# 会话签名密钥（不设置则从 ADMIN_PASSWORD 派生；更换它可使所有登录态失效）
# SESSION_SECRET=随机长字符串

# 新评论推送（三选多，全部不配置则只用站内通知）
# BARK_URL=https://api.day.app/你的Key
# SERVERCHAN_SENDKEY=你的SendKey
# RESEND_API_KEY=re_xxx
# NOTIFY_EMAIL=you@example.com
# NOTIFY_FROM=START Blog <blog@your-domain.com>
